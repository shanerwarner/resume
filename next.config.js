/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */

const nextConfig = {}

module.exports = nextConfig;

const config = {
  experimental: {
    serverActions: true,
  },
};

module.exports = config;