/** @type {import('next').NextConfig} */
/** @type {import('next').NextConfig} */

const nextConfig = {}



const config = {
  experimental: {
    serverActions: true,
  },
};

module.exports = config;
module.exports = nextConfig;